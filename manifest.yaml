---
alacritty:
  - src: alacritty.yml
    links: 
      - ${XDG_CONFIG_HOME}/alacritty/alacritty.yml


asdf:
  - src: tool-versions
    links:
      - ${HOME}/.tool-versions
  - src: asdfrc
    links:
      - ${XDG_CONFIG_HOME}/asdf/asdfrc


bash:
  - src: bashrc
    links:
      - ${HOME}/.bashrc

  - src: bash_profile
    links: 
      - ${HOME}/.bash_profile


readline:
  - src: inputrc
    links: 
      - ${HOME}/.inputrc


bat:
  - src: bat.conf
    links:
      - ${XDG_CONFIG_HOME}/bat/bat.conf


zsh:
  - src: zshenv
    links:
      - ${HOME}/.zshenv

  - src: zprofile
    links: 
      - ${XDG_CONFIG_HOME}/zsh/.zprofile

  - src: zshrc
    links: 
      - ${XDG_CONFIG_HOME}/zsh/.zshrc

  - src: zlogin
    links: 
      - ${XDG_CONFIG_HOME}/zsh/.zlogin

  - src: zlogout
    links: 
      - ${XDG_CONFIG_HOME}/zsh/.zlogout

  - src: aliases.zsh
    links: 
      - ${XDG_CONFIG_HOME}/zsh/aliases.zsh
      - ${XDG_CONFIG_HOME}/bash/aliases.sh
  - src: functions.zsh
    links: 
      - ${XDG_CONFIG_HOME}/zsh/functions.zsh
      - ${XDG_CONFIG_HOME}/bash/functions.sh


starship:
  - src: starship.toml
    links: 
      - ${XDG_CONFIG_HOME}/starship.toml


ssh:
  - src: config
    links: 
      - ${HOME}/.ssh/config


neovim:
  - src: init.vim
    links: 
      - ${HOME}/.vimrc
      - ${XDG_CONFIG_HOME}/nvim/init.vim


tmux:
  - src: tmux.conf
    links:
      - ${XDG_CONFIG_HOME}/tmux/tmux.conf


git:
  - src: config
    links: 
      - ${XDG_CONFIG_HOME}/git/config

  - src: ignore
    links: 
      - ${XDG_CONFIG_HOME}/git/ignore


python:
  - src: pythonrc.py
    links: 
      - ${HOME}/.pythonrc.py


pylint:
  - src: pylintrc
    links: 
      - ${XDG_CONFIG_HOME}/pylintrc


pywal:
  - src: modified_monokai.json
    links:
      - ${XDG_CONFIG_HOME}/pywal/modified_monokai.json


flake8:
  - src: flake8
    links:
      - ${XDG_CONFIG_HOME}/flake8


sqlite:
  - src: sqliterc
    links: 
      - ${HOME}/.sqliterc

lsd:
  - src: config.yaml
    links:
      - ${XDG_CONFIG_HOME}/lsd/config.yaml
  - src: my-theme.yaml
    links:
      - ${XDG_CONFIG_HOME}/lsd/themes/adam.yaml

sublime:  # TODO: if mac use mac settings path
  - src: sublime-text-3
    links: 
      - ${XDG_CONFIG_HOME}/sublime-text-3/Packages/User/Preferences.sublime-settings


vs-code:
  - src: settings.json
    links: 
      - ${XDG_CONFIG_HOME}/Code/User/settings.json


gdb:
  - src: gdbinit
    links: 
      - ${HOME}/.gdbinit

sway:
  - src: config
    links:
      - ${XDG_CONFIG_HOME}/sway/config

gammastep:
  - src: config.ini
    links:
      - ${XDG_CONFIG_HOME}/gammastep/config.ini
  - src: gammastep.service
    links:
      - ${XDG_CONFIG_HOME}/systemd/user/gammastep.service

systemd:
  - src: sway-session.target
    links:
      - ${XDG_CONFIG_HOME}/systemd/user/sway-session.target

swayidle:
  - src: swayidle.service
    links:
      - "${XDG_CONFIG_HOME}/systemd/user/swayidle.service"
